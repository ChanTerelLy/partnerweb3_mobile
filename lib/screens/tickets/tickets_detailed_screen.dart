import 'package:flutter/material.dart';

class TicketsDetailedScreen extends StatefulWidget {
  final Map arguments;
  TicketsDetailedScreen(this.arguments);

  @override
  _TicketsDetailedScreenState createState() => _TicketsDetailedScreenState();
}

class _TicketsDetailedScreenState extends State<TicketsDetailedScreen> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
